name: BAOLSAT Daily Intelligence 🛰️

on:
  schedule:
    - cron: '0 8 * * *'  # 8h UTC = 8h à Dakar (en 2026, vérifie le décalage)
  workflow_dispatch:      # Permet de lancer le rapport manuellement depuis GitHub

jobs:
  intelligence_agro_spatiale:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Récupération du code source
        uses: actions/checkout@v4

      - name: 🐍 Configuration de Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Installation des dépendances
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: 🛰️ Analyse Copernicus & Envoi Rapport
        env:
          # Secrets à configurer dans les paramètres GitHub de ton dépôt
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_ID: ${{ secrets.TG_ID }}
          COPERNICUS_USERNAME: ${{ secrets.COPERNICUS_USERNAME }}
          COPERNICUS_PASSWORD: ${{ secrets.COPERNICUS_PASSWORD }}
          # On peut aussi passer l'URL de ton serveur Flask si tu veux poster l'alerte
          BAOLSAT_SERVER_URL: ${{ secrets.BAOLSAT_SERVER_URL }}
        run: python script_baolsat_intelligence.py
