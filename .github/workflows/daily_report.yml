name: BAOLSAT 2026 CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4

      - name: 🐍 Configuration Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Installation des dépendances
        run: |
          python -m pip install --upgrade pip
          # On installe le requirements.txt qui est maintenant dans le dossier backend
          pip install -r backend/requirements.txt

      - name: 🔍 Vérification du Core Engine (NDVI)
        run: |
          # Correction du chemin : on pointe vers satellite_engine
          python -m py_compile satellite_engine/ndvi_engine.py

      - name: 🌐 Vérification du Backend Flask
        run: |
          # Correction du chemin : on pointe vers backend
          python -m py_compile backend/app.py

      - name: 🚀 Test de lancement (Simulation)
        run: |
          cd backend
          # On lance Flask en arrière-plan pour vérifier qu'il ne crash pas au démarrage
          python app.py & 
          sleep 10
          kill $!
